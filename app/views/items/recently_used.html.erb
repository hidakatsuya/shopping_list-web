<%= turbo_frame_tag 'recently_used_items' do %>
  <datalist id="recently-used-item-list">
  <% @item_names.each do |item_name| %>
    <option value="<%= item_name %>">
  <% end %>
  </datalist>
<% end %>
