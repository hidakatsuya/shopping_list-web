<main class="container">
  <%= link_to sanitize("&larr; Back to item"), item_path(@item) %>

  <div class="header">
    <h1>Edit item</h1>
  </div>

  <%= turbo_frame_tag @item do %>
    <%= render "form", item: @item %>
  <% end %>
</main>
